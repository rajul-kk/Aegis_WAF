# Core CAMEL-AI
camel-ai>=0.2.0

# LLM Backend (OpenAI-compatible for Llama 3.3 via NVIDIA NIM / Groq / Together)
openai>=1.0.0

# NeMo Guardrails
nemoguardrails>=0.10.0

# Fast Regex Engine (used by scanner)
google-re2>=1.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
httpx>=0.25.0
tenacity>=8.0.0

# Frontend dashboard
streamlit>=1.38.0
pandas>=2.0.0

# API backend
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Persistence
redis>=5.0.0

# Test-only (simulates Redis in-process, no server needed for CI/local tests)
fakeredis>=2.20.0
